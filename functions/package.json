{
  "name": "synapsys-note-functions",
  "version": "1.0.0",
  "description": "Cloud Functions do Synapsys Note: limpeza agendada da lixeira e da quarentena de mídia e exclusão definitiva de notas.",
  "private": true,
  "main": "lib/index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "sync": "node ../scripts/sync-functions-shared.mjs",
    "build": "tsc",
    "build:watch": "tsc --watch",
    "serve": "npm run sync && npm run build && firebase emulators:start --only functions,firestore,storage,auth",
    "shell": "npm run sync && npm run build && firebase functions:shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log",
    "typecheck": "node ../scripts/sync-functions-shared.mjs --check && tsc --noEmit"
  },
  "dependencies": {
    "firebase-admin": "^13.6.0",
    "firebase-functions": "^6.6.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.9.2"
  }
}
